# Observatoire Économique Espace Sabaudo — variables d'environnement
# Copier ce fichier en `.env` et renseigner les valeurs réelles.
# Le fichier `.env` est ignoré par git (voir .gitignore).

# Clé API Anthropic (https://console.anthropic.com/)
ANTHROPIC_API_KEY=

# Chemin du fichier d'identifiants OAuth2 Gmail/Drive (téléchargé depuis Google Cloud Console)
GMAIL_CREDENTIALS_PATH=config/credentials.json

# Identifiant du dossier Google Drive « Observatoire Économique »
DRIVE_FOLDER_ID=1OWmS4oC7rw2BGQtInnuM2_8kE0wkXv53

# Sous-dossiers Drive où déposer les résultats (créés automatiquement au besoin)
DRIVE_VEILLE_BRUTE_SUBFOLDER=01_Veille_brute
DRIVE_VEILLE_TRAITEE_SUBFOLDER=02_Veille_traitee

# --- Brevo (brouillon de newsletter automatique — AUCUN envoi auto) ---
# Clé API : Brevo > Paramètres > SMTP & API > Clés API
BREVO_API_KEY=
# Nom affiché de l'expéditeur
BREVO_SENDER_NAME=Cultura Sabauda
# Email expéditeur — DOIT être un expéditeur validé dans Brevo
BREVO_SENDER_EMAIL=
# Id(s) de la liste de destinataires (un seul : 3 — ou plusieurs : 3,4)
# Pour trouver l'id : python scripts/push_brevo.py --check
BREVO_LIST_ID=
# (Option) Logos Cultura Sabauda hébergés (bibliothèque média Brevo) :
#   BREVO_LOGO_URL  : logo HORIZONTAL → en-tête (masthead)
#   BREVO_PICTO_URL : pictogramme     → pied de page
# Si vide : la newsletter s'affiche sans ces visuels (les favicons des sources restent).
BREVO_LOGO_URL=
BREVO_PICTO_URL=
# (Option) URL PUBLIQUE du tableau de bord « toute la veille ». Si renseignée, la
# newsletter affiche un bouton « Voir toute la veille → » vers cette page.
DASHBOARD_URL=https://culturasabauda.eu/observatoire/
# (Option) Publication AUTOMATIQUE du dashboard sur l'hébergement web (à chaque
# synthèse), déposé sous le nom index.html dans DASHBOARD_FTP_DIR.
# Protocole : sftp (défaut — Gandi Simple Hosting) | ftp | ftps.
DASHBOARD_FTP_PROTO=sftp
DASHBOARD_FTP_HOST=
DASHBOARD_FTP_USER=
DASHBOARD_FTP_PASS=
# Chemin ABSOLU du dossier web cible (à confirmer chez Gandi : souvent sous
# .../vhosts/default/htdocs). Ex. /srv/data/web/vhosts/default/htdocs/observatoire
DASHBOARD_FTP_DIR=
# Port (optionnel : 22 par défaut en sftp, 21 en ftp)
DASHBOARD_FTP_PORT=

# --- Optionnels (valeurs par défaut entre parenthèses) ---
# Modèle Anthropic pour la SYNTHÈSE éditoriale (défaut : claude-opus-4-8 — le plus
# capable ; 1 appel/semaine). Surcharge possible (ex. claude-sonnet-4-6 pour réduire le coût).
# ANTHROPIC_MODEL=claude-opus-4-8
# Modèle pour la RECHERCHE de liens officiels (défaut : claude-haiku-4-5 — tâche simple,
# ≈10 appels/semaine, donc on prend le moins cher).
# OFFICIAL_SEARCH_MODEL=claude-haiku-4-5
# Liens « site officiel » pour les brèves radar : retrouve l'article PRÉCIS sur le
# domaine officiel de l'acteur (config/official_links.txt) via recherche web.
# Désactivé par défaut (coût/temps). Mettre à 1 pour activer.
# OFFICIAL_LINK_SEARCH=0
# Fenêtre de collecte Gmail en jours pour le cron hebdomadaire (8)
# GMAIL_LOOKBACK_DAYS=8
# Délai max (secondes) pour la collecte d'un flux RSS (30)
# RSS_TIMEOUT=30

# --- Page admin privée (scripts/admin_server.py — bouton « relancer la newsletter ») ---
# OBLIGATOIRE pour démarrer le service (aucun mot de passe par défaut, par sécurité).
ADMIN_USER=
ADMIN_PASS=
# Interface/port d'écoute (défaut : 127.0.0.1:8099, à exposer derrière Traefik/HTTPS).
# ADMIN_HOST=127.0.0.1
# ADMIN_PORT=8099
# Sous-chemin si la page est servie derrière un proxy (ex. /admin → https://…/admin).
# Doit correspondre au PathPrefix du routeur Traefik. Vide = page à la racine.
# ADMIN_BASE_PATH=/admin
# Interpréteur Python utilisé pour lancer les tâches (défaut : celui du service).
# ADMIN_PYTHON=/home/user/observatoire-business-sabaudo/.venv/bin/python
